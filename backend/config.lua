local _G = _G or _ENV --Backwards compatability

_G.config = {
  secret = "g59qGlCSQTECFGdjeZxm", --Used for token validation & creation
                                   --should be replaced by a new, unique version in deployment
}
